language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -f -t givepeasachance/docker-react Dockerfile.dev .

script:
  - docker run -e CI=true givepeasachance/docker-react npm run test